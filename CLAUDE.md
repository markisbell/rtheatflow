# CLAUDE.md — rtheatflow development log & agent handoff

## What this repo is

Real-time district-heating simulation platform on **pandapipes 0.14.0**, structurally cloned from the blueprint **[markisbell/rtpowerflow](https://github.com/markisbell/rtpowerflow)** (project "netzsim"). **[SPEC.md](SPEC.md) is the binding build specification** — read it fully before writing code. When SPEC.md and your prior API knowledge disagree, SPEC.md wins: its pandapipes facts were verified at runtime against the pinned version.

## Binding rules

1. **✅ RECONCILED (2026-07-16):** the SPEC §0 rules repo https://github.com/markisbell/claude-memory is the owner's private cross-project Claude memory (accessible via the machine's Git Credential Manager; it was 404 only for anonymous access). The binding do's and don'ts live in its `user-workflow-prefs.md`; the M1–M7 practices were reviewed against them and are consistent (verify live ✓, no locks — self-healing solve ✓, German-first UI with domain vocabulary ✓, regression tests pinned before refactorings ✓). Rules to observe going forward: **commit/push only on the user's request in interactive sessions** (milestone agents committed under explicit per-milestone authorization). netzsim's i18n key-parity convention is **already satisfied**: `ui/src/i18n.ts` declares `const en: typeof de` (German-first direction — DE is the authoring reference), verified 2026-07-16 by a deliberate DE-only probe key failing `tsc` with TS2741. This project's own state file in that repo is `rtheatflow-platform-state.md`.
2. Mirror netzsim structure/naming/behavior (SPEC §9). When in doubt: do what rtpowerflow does.
3. Every milestone (SPEC §12) ends with working, tested, committed code before the next begins.

## Non-negotiable technical pins (evidence in SPEC Appendix A/B)

- `pandapipes==0.14.0` — do **not** pin pandapower separately (0.14.0 pins `pandapower==3.3.3` itself).
- Solve mode: `bidirectional` with the SPEC §3.3 retry ladder. Never `nonlinear_method="automatic"` in bidirectional (raises ValueError).
- Exactly **one** `circ_pump_const_pressure` per net (409 on second).
- Always pass `text_k` explicitly on every pipe (the default 0 means 0 K ambient → ~4× fake losses).
- Pipe-loss formula is **direction-aware** (inlet = `t_from` if mdot ≥ 0 else `t_to`); the naive formula overcounts ~20 % on meshed nets.
- Feed-in KPI = `mdot·cp̄·ΔT`, **not** the raw `res_circ_pump_pressure.qext_w` column (enthalpy form, ~+4.7 %).
- Platform warm start after every converged step (write `res_junction` back into `junction.pn_bar`/`tfluid_k`); reset to supply-temp init after swaps/failures.
- Zero-flow guard: consumers floored ≥ 200–500 W or switched out of service atomically; bypasses per SPEC §3.2.

## Development log

### 2026-07-15 — Project inception (pre-code)
- SPEC.md authored, grounded in multi-agent research on the blueprint repo and pandapipes 0.14.0 source; adversarially verified (18 findings fixed).
- Core validated on the target machine (Win 11, Python 3.14): known-answer fixture reproduced exactly (balance −0.04 %); solve benchmarks 21 ms (8 junctions) / 33 ms (`schutterwald_heat`, 488 junctions); retry ladder validated on the hard `treturn` case (`alpha=0.5` converges); transient smoke test passed.
- Realistic-year physics validation: re-parametrized schutterwald reproduces literature loss ratios endogenously (12.9 % at 1.5 MWh/m·a; 19.1 % rural; 9.1 % urban; winter ~7.5 % vs summer ~45 % instantaneous). Discovered: shipped example net has all pipes at 800 mm inner diameter — benchmark-only, re-parametrize as seed.
- Profile toolbox verified (SPEC §4.5): demandlib VDI 4655 (SH) + OpenDHW (stochastic DHW); LPG/pylpg viable for DHW but its space-heating output is degree-day-only.
- Repo scaffolded; `scripts/generate_profiles.py` generates the first 3 archetypes (EFH alt/saniert, MFH) into `data/profiles/` — 15-min, seed-salted DHW variants, annual sums verified against targets.
- Next: **M1 — headless core** (SPEC §12).

### 2026-07-15 — M1: headless core (branch `m1-headless-core`)

**Built** (SPEC §12 M1; module map per §9.1): `config.py` (pydantic-settings, `RTHEATFLOW_`), `models.py`/`data_loader.py`/`net_inputs.py` (five-file contract with pydantic v2 + cross-validation: node refs, array lengths, horizon alignment, exactly-one-slack, reachability from slack, dead-end zero-flow rejection), `network_builder.py` (build-once supply/return pair expansion, explicit `text_k` everywhere, ISOPLUS std types or 0.14 custom params, dense `[n_elements, ticks]` `ProfileArrays` + explicit `NetIndex` records, qext floor), `weather.py` (§4.5 degree-hour override scaling with ε-guarded summer regime), `heating_curve.py` (§4.2 + 3G/4G presets), `simulator.py` (§3.3 retry ladder with `SOLVER_ITER` semantics + catch-all arm, §3.4 platform warm start with supply-temp reset on failure, §3.6 derived quantities: direction-aware losses, `q_feed = mdot·c̄p·ΔT`, worst-point Δp+argmin, pump P_el, balance_err; §6 `StepResult`, °C on the wire via `_r()`), `engine.py` (asyncio tick loop, `to_thread` solve, Event pause/resume, seek/seek_day/interval floor 0.01 s, off-thread `reconfigure`; `HeadlessStore` stands in for M2's `StateStore`), `sensors.py` (`_r()` only; `MeasurementSet` default-preset stub deferred to M2 per §8a). Fixture: SPEC Appendix A as five files under `data/networks/appendix_a/`.
- **Tests: 51 passed** (~14 s). Known-answer test reproduces every §11 pin exactly on this machine (A mdot 0.674357/t_outlet 328.150; B Δt 30.0000/mdot 0.397762; C mdot 0.2500; pump 1.322118 kg/s; return 324.355 K; end supply 351.837 K; losses 27.258 kW; q_feed 187.182 kW vs raw enthalpy column 195.965 kW; balance −0.04 %). Bidirectional-necessity, retry-ladder hard case (tier 2 α=0.5), forced non-convergence (+ recovery), zero-flow guard, day wrap, override scaling, pandapipes pins all green.
- **Benchmarks** (`docs/BENCHMARKS.md`): warm medians ~27 ms (fixture) / ~21 ms (`schutterwald_heat`), first solve incl. numba JIT ~7 s; significant run-to-run variance on this laptop-class CPU (20–35 ms) — indicative, not thresholds.
- **Deviations from SPEC (documented, deliberate):**
  1. `consumers.json` accepts `controlled_mdot_kg_per_s` as a third partner to `qext_w` (pandapipes pair 3) beyond the §5 sketch's `treturn_k|deltat_k` — required to express Appendix A consumer C and the §3.2 canonical bypass in the contract.
  2. New setting `RTHEATFLOW_MIN_QEXT_W` (default 500 W) — §6's config list has no knob for the §3.2 floor, but §3.2 mandates a validated, generous floor; documented in `.env.example`.
  3. `pump_mass` producers require `p_flow_bar` + `t_flow_k` in addition to §5's `mdot_flow_kg_per_s` (the `create_circ_pump_const_mass_flow` call needs them).
  4. Profile resampling choice (§4.5 "pick one"): staircase, documented in `network_builder.py`.
- **Spec refinement discovered (not an error):** Appendix B item 2's "`nonlinear_method="automatic"` raises `ValueError` under bidirectional" holds only when the damping adaptation *engages* (errors increase mid-iteration; unfixed source TODO in `pipeflow.finalize_iteration` → broadcast shape error). Easy nets can pass without raising; the hard `schutterwald_heat(70, treturn=45)` case reproduces it. Conclusion unchanged (ladder never uses it); pinned in `tests/test_pandapipes_pins.py` on the hard case.
- Next: **M2 — API** (FastAPI + WS, StateStore, `MeasurementSet` default-preset stub, API-surface pinning test).

### 2026-07-15 — M2: API (branch `m2-api`)

**Built** (SPEC §12 M2; §6/§7/§8a/§9.1/§9.3):
- `state.py` — `StateStore`: `latest` + `deque(maxlen=history_size)` (public attrs, M1-test-compatible), WS subscriber set (`asyncio.Lock` for subscriber mutation only), optional recorder `sink` hook (M6), `reset()` keeps subscribers (grid swap). **One** `asdict()`+`_project()` path serves REST `/state`, `/history` and every WS frame; strict mode (`RTHEATFLOW_EXPOSE_GROUND_TRUTH=false`) strips `_TRUTH_KEYS` (junctions/pipes/consumers/summary) and blanks the free-text `error` detail. Dead sockets discarded on send failure. Engine's M1 `HeadlessStore` removed.
- `sensors.py` — §8a interim `MeasurementSet`: default preset `all_consumers` + plant SCADA; `observe(truth_payload) -> (measurements, observed_summary)` is a *projection* of collected truth (heat-meter channels only: q/mdot/T_s/T_r/dp), aggregates over metered elements only. Wired into `Simulator._collect` — every frame carries the observed layer, so strict mode is meaningful and the M4 Δp controller reads observed from day one. Presets `all_consumers|plant_only|clear`; `key_points` + placement CRUD + fidelity modes stay M5.
- `simulator.py` — minimal M2 runtime CRUD: `add_heat_exchanger`/`remove_heat_exchanger` (direct net mutation, dispatch-profile row append/delete, init reset per §3.4, racing solves self-heal per §3.3; verified: 20 kW feed-in → plant backs off 187.2→173.8 kW, balance 0.07 %).
- `api/` — `runtime.py` (App singleton, `API_VERSION = "0.2.0"`, shared `status_payload`, `/network` topology builder with trench-grouped supply/return pipe ids + geometry fallback), `__init__.py` (`create_app(settings, network_dir)`, lifespan loads `data/networks/appendix_a` via the five-file loader → Simulator+StateStore+RealtimeEngine, `RTHEATFLOW_AUTOSTART`, CORS from settings, Swagger at /docs), `core.py` (GET `/` built-in WS-fed HTML monitor, `/health`, `/status`, `/network`, `/state` **404 pre-solve only**, `/history?limit=` 1..10000 default 96, `WS /ws` latest-on-connect then one frame per solved step), `control.py` (start/pause/resume/seek/seekday/interval, each returns fresh status), `weather.py` (GET + PUT/DELETE override, 422 outside −30…45 °C), `plant.py` (GET/POST `/heatingcurve` + 3G/4G presets; `/dpcontrol` deliberately absent until M4 — no stub routes), `producers.py` (GET `/producers`, POST `/producer`: **409 second slack**, 400 missing kind-specific fields/unknown node/kind, functional `heat_exchanger` placement; DELETE `/producer/{id}`: hx removed, slack 409, pump_mass 400-M4, unknown 404). `main.py` uvicorn entry (§9.3). `scripts/gen_api_doc.py` → generated `docs/API.md` (21 routes), staleness-checked by the surface test.
- **Tests: 76 passed** (51 M1 kept green + 25 M2, ~15 s): route-inventory pin, `/state` 404→§6-exact shape, WS latest+stream with frames identical to `/state`, control verbs actually affect the engine (pause freezes production), history-limit 422s, 409/400/422 conventions, functional hx add/remove over the API, override rescales SH ×1.6, 4G preset drops plant t_flow in the physics, strict-mode gating over REST+WS, non-convergence-is-data over the API (sabotaged solver → `converged=false` frames, loop alive, never 500). Verified against a real uvicorn instance too (health/state/history/monitor/WS).
- **Discoveries (folded into SPEC §3.6/§6/§10.1):**
  1. **`res_heat_exchanger` has NO `qext_w` column at runtime 0.14.0** (8 branch cols only) — §3.6's total-feed-in formula referenced it; a latent M1 `_collect` bug (unexercised: the fixture had no hx) surfaced on the first placement. Fixed: dispatch read from the component table (authoritative anyway — it's a fixed input setpoint).
  2. **Producer wire ids are now platform-unique `pid`s** — pandapipes element indices are per-component-table and collide across kinds (slack 0 vs heat_exchanger 0 made `DELETE /producer/0` ambiguous). `producer_meta` carries `pid` + internal `element`.
  3. FastAPI ≥0.139 nests included routers lazily (`_IncludedRouter`); route enumeration for gen_api_doc/surface test goes through the OpenAPI schema + a WS-route walk, not `app.routes` isinstance checks.
- **Deviations (documented, deliberate):** weather-override plausible band set to −30…45 °C (§7 names no band); `POST /producer` scoped to `heat_exchanger` (pump_mass placement → 400 with M4 pointer; full CRUD incl. `POST /producer/{id}/config` is M4); default network is the `DEFAULT_NETWORK_ID = "appendix_a"` constant until the M4 catalog (superseded by `RTHEATFLOW_DEFAULT_NETWORK`, M3); `httpx2` added to dev extras (starlette ≥1.3 TestClient deprecation). M5-area routes (`/measurements*`), storage/consumer/bypass, networks/scenarios/recording are absent by design — the surface test pins only what exists.
- Next: **M3 — UI map** (Leaflet trench rendering, live color layers, time controls, DE/EN).

### 2026-07-16 — M3: UI map (branch `m3-ui-map`)

**Built** (SPEC §12 M3; §8/§9.2/§9.3):
- `ui/` — React 18.3 + TypeScript ~5.6 (strict) + Vite 6 + **raw Leaflet 1.9.4** (no react-leaflet), i18next/react-i18next, **no** chart/state/router libs (§2). `vite.config.ts` proxies `/api`→`http://127.0.0.1:8000` with prefix rewrite and `/ws` with `ws:true` — target pinned to 127.0.0.1, never `localhost` (Windows IPv6 trap, §9.3). Build = `tsc && vite build` (type gate); `npx vitest run` for unit tests.
- `src/types.ts` — StepResult/Topology/EngineStatus mirroring the M2 wire exactly; strict mode modeled as *absent* truth keys (`junctions?/pipes?/consumers?/summary?`). `src/api.ts` — typed wrappers for the 21 M2 routes (control verbs take **JSON bodies** `{step}/{day}/{seconds}` — deviation from the blueprint's query params, matching our M2 API) + `wsUrl()`. `src/useStepStream.ts` — verbatim blueprint WS hook (1.5 s reconnect, StrictMode per-socket identity guards, malformed frames ignored).
- `src/scales.ts` + 13 vitest tests — domain-anchored ramps (§8): supply warm ramp **full-hot exactly at the active `t_flow_design`** (anchor read per frame from `controls.heating_curve`), return cool ramp 25–70 °C (blue→violet so low-ΔT syndrome pops), velocity ramp with amber anchor exactly at 1.5 m/s / red at 3, Δp traffic light red strictly below `DP_MIN_BAR`; dedicated `UNOBSERVED`/`UNOBSERVED_LINE` greys + `6 6` dash that **no healthy ramp value can produce** (pinned by test).
- `src/components/MapDiagram.tsx` — `preferCanvas: true`, CARTO light/dark + OSM attribution, **one polyline per trench** (shared geometry from `/network`), all vector layers created once per topology and restyled per frame via refs (`.setStyle()` only); stroke width by `|mdot|` (sqrt, snapshot-relative); consumers as circleMarkers sized by `q_design_w`, plant as amber station marker + 🏭 divIcon; **live popups** (content functions read the latest frame through a ref; open popups refresh per frame); per-layer colorbar legend; on-map layer switch mirroring the Ansicht menu. Measured view: pipes carry no meter → UNOBSERVED dashed; consumers colored from `measurements.consumers` only; no frame yet → everything unknown-grey (honest cold start).
- `src/views/LiveHeatFlow.tsx` — map + resizable sidebar (clock, WS status, `OverviewSection`: feed-in/demand/losses kW+%/pump P_el/plant T_flow+T_return/**heating-curve setpoint evaluated client-side** (§4.2 formula)/worst-point Δp + critical consumer/solver badge ok|degraded|failed/solve time; observed variant aggregates `observed_summary` + coverage) + transport bar (play/pause, time-of-day seek slider, day slider when `n_days>1`, speed slider 0.1–1 s); every verb re-syncs from the returned engine status, status re-polled every 2 s.
- `src/App.tsx` + `src/i18n.ts` — topbar shell: brand, Ansicht (layer radios) + Hilfe menus, permanent Sicht segment **Realität/Gemessen/Schätzung** (est disabled until M7) with the §8 fallback chain (`truth→observed` when the server strips `summary`), network chip (name · consumers · trench km), DE/EN switch. German default / English fallback, feature-prefixed keys, complete for every shipped string.
- **Demo network `data/networks/demo_dorf/`** + generator `scripts/generate_demo_dorf.py` (deterministic, seeded): village at 49.004 N / 8.397 E — 1 plant ("Heizzentrale", 3G slack 6 bar/Δ2.5 bar with 85/70 heating curve n=1.3), **11 consumers** (6× EFH_ALT, 3× EFH_SAN, 2× MFH) wired from the committed `data/profiles/` archetypes (90th-percentile-SH winter day, DHW-variant rotation, seeded ±30 min shift + 0.92–1.08 amplitude jitter), archetype-specific `treturn` (55/50/45 °C), weather **inverted from the day's SH level** via the §4.5 degree-hour factor (override math self-consistent) + diurnal sinusoid, pipe DNs sized from downstream design loads to ≤1 m/s (ISOPLUS STD types, DN20–DN50), street-like trench geometry, every leaf a consumer (no dead ends). 182 kW design / 0.95 km trench (LHD ≈ 0.9 MWh/(m·a) — honest rural). Solves tier 1 at every probed step, |balance| ≤ 0.03 %, losses 14–23 % instantaneous on the mild demo day.
- Backend touch-up: `RTHEATFLOW_DEFAULT_NETWORK` setting (default `demo_dorf`) replaces the `DEFAULT_NETWORK_ID` constant; tests keep loading `appendix_a` explicitly via `network_dir`.
- **Tests: 82 backend** (76 kept green + 6 `test_demo_dorf.py`: contract/geography/balance ≤1 %/velocity bound/curve-following/default-app wiring) + **13 UI vitest** (scales anchors + unknown-grey pin); `npm run build` green (tsc strict).
- **Verified end-to-end** (uvicorn + Vite dev, Chrome via browser tooling): map renders demo_dorf trenches on CARTO tiles at zoom 17; layer switching confirmed by canvas pixel histograms (supply → YlOrRd warm tones exactly matching the ramp stops for 77–80 °C on the 60–85 domain; return → blues; velocity → greens, village runs 0.2–0.9 m/s; Δp → neutral trenches + green consumer markers 1.0–1.8 bar) and per-layer legends; measured view flips pipes to `#2b323c` dash `6 6` with consumers still colored (11/11 metered) and the Übersicht switching to meter aggregates; trench popup shows live values in reality view ("Vorlauf 78,4 → 78 °C · 1,113 kg/s · Verlust 1,85 kW") and "keine Messung — unbekannt" in measured view, **refreshing while open**; DE/EN flips every visible string; pause/seek(420→07:00)/speed(0.1 s)/resume verified against `/status`; UI clock tracks the backend latest frame exactly even at 10 steps/s (≤1 frame behind, WS-driven).
- **Discoveries:**
  1. **Map-mount sizing race:** when Leaflet mounts in the same React commit that lays out the CSS grid, the container can be 0-sized at construction and `fitBounds` lands at world zoom 0; `invalidateSize()` alone does not re-fit. Fix: re-run `fitBounds` inside the deferred (80 ms) `invalidateSize` timer.
  2. Browser-tooling caveat for future agents: the embedded browser pane runs the tab **hidden** — Chrome suspends `requestAnimationFrame`, so Leaflet's canvas renderer never repaints after load (and screenshots time out). The app is fine in a real browser; for automated pixel checks force `renderer._redraw()` after interactions.
- **Deviations (documented, deliberate):** control verbs use JSON bodies (M2 API contract) where the blueprint used query params; Δp layer colors consumer markers and mutes trenches (§8 defines it as a consumer-marker layer); `MeasurementPanel`/`ElementMenu`/`ProfileGraph`/`Sparkline` ports and the pressure diagram are M4/M5 scope — `scales.test.ts` carries the blueprint's Sparkline-test convention until then; speed slider range 0.1–1 s (blueprint parity).
- Next: **M4 — equipment & scenarios** (placement menu, producer/storage/bypass CRUD, Δp controller, network catalog + NetzStudio, weather knob UI).

### 2026-07-16 — M4: equipment & scenarios (branch `m4-equipment-scenarios`)

**Built** (SPEC §12 M4; §3.2/§4.3–§4.6/§7/§8/§9.1/§9.2):
- `dp_control.py` — §4.3 Schlechtpunktregelung: **one clamped proportional step per tick, after the solve** (`plift += clamp(K·(dp_set−dp_worst), ±max_step)`, K=0.5, max_step 0.05 bar/tick, plift range clamp 0.1–10 bar), modes `controlled`/`fixed` ("ungeregelte Pumpe"); consumes **only** `observed_summary.dp_worst_bar` (§8a blindness principle: `None` → hold, pinned by test — with preset `plant_only` the controller never touches the truth's worst point). `GET/POST /dpcontrol`, setpoint band 0.3–2.0 bar → 422, fixed-mode direct `plift_bar` knob.
- `storage.py` + Simulator bookkeeping — §4.4 buffer storage: charge = `heat_consumer(qext_w, treturn_k=t_bottom)`, discharge = `circ_pump_const_mass_flow(return_j, supply_j, t_flow_k=t_top)`, **exactly one branch active per tick** (requested mode after power/capacity limits — a full store idles itself), idle = §3.2 floor pair (0.02 kg/s + 100 W) so the stub pipes never hit zero flow; SoC integrates the *realized* heat (charge: qext setpoint; discharge: `mdot·cp·(t_out−t_from)` from `res_circ_pump_mass`) with one-way η=0.95; idle standby is a standing loss (drawn from the net, not credited to SoC). REST per §7 (`/storages`, `POST /storage`, `/storage/{id}/config` incl. live mode/size, `DELETE`). Storage flows enter the balance: `q_feed(plant+hx+pm+discharge) ≈ q_cons + q_loss + q_charge`; summary gains `q_storage_kw`.
- Producers — `pump_mass` placement/removal/config CRUD complete; `producers.py` = §4.4 platform dispatch models on the slack (`POST /producer/{id}/config {plant_kind: boiler|chp|heat_pump, eta, pq_ratio, eta_g, t_cold_source}`): HP `COP = η_g·T_hot/(T_hot−T_cold)` in Kelvin (η_g 0.5, T_cold live from `t_amb`/`t_ground`, lift floored at 1 K), per tick `cop`/`p_el_kw` in the frame (formula pinned by test); CHP `p_el_kw = 0.5·q`; boiler `p_fuel_kw = q/η`. hx re-dispatch via config.
- Consumers/bypass — `POST /consumer {node, archetype|q_kw, treturn_c}` places at an existing trench node (archetype profiles cut from the `data/profiles/` year cache: percentile day window, DHW-variant rotation, seeded jitter — `consumers.py`), `POST /bypass` = the §3.2 canonical pair, `DELETE /consumer/{id}` (**409 for the last consumer**). The §3.2 zero-flow floor is now **per-row**: mdot-pair rows (bypasses, appendix_a C) keep their configured standby qext — a 100 W bypass stays 100 W, fixed flow carries no singularity risk.
- `network_catalog.py` + `data/network_library.json` (demo_dorf rural 15 nodes 0.953 km · appendix_a abstract) — `GET /networks`, `GET /networks/{id}` (preview stats incl. design load, LHD), `POST /config/apply {network_id, loadgen?}` (= `engine.reconfigure` swap + store reset + topology/active-meta rebuild), `GET /config/active`. **`POST /networks/import` deferred to M6** (with the user_networks scan) — documented, no stub route.
- `loadgen/` — §4.5 policy (`archetypes|mode|seed|scale|day_percentile|jitter|dhw_variants|temperature_preset`) → deterministic assignment; `POST /loadgen/assign` previews (assignment table, load-duration curve, KPIs: design/peak/annual, trench km, **linear heat density** with the ≥1–1.5 MWh/(m·a) rule) without applying; weather re-derived by inverting the §4.5 degree-hour factor from the chosen archetype day (override math stays self-consistent); `temperature_preset` 3G/4G couples archetype return temps (55/45/50 vs 45/35/40 °C) **and** the §4.2 curve preset on apply.
- `scenarios.py` + API — §4.6 **recipes, not snapshots**: network id + loadgen policy + runtime producers/storages (config, not state) + consumer op log (placement recipes incl. removals, replayed in order) + heating-curve/Δp/plant config + weather override + engine clock; `measurements` field reserved for M5 (interim preset saved). Load replays tolerantly per entry (blueprint; non-atomic accepted §9.3) then starts the engine. Ships 2 reference scenarios: `demo-dorf-3g-winter` / `demo-dorf-4g-vergleich` (same seed 42 — direct comparison; 4G runs a ground-source HP).
- UI — `ElementMenu` (§8 grammar: right-click any element/node → element actions [pin, remove, storage mode, plant-kind picker] + "Hier platzieren" [hx/pump/storage/bypass/consumer-with-archetype-picker]; two-page menu), Ctrl-click pins `PinnedSection`s (`EquipmentControls.tsx`: live values + config knobs, storage SoC bar); `MapDiagram` gains grey trench-node markers, bypass 🔀 markers, per-frame-diffed equipment divIcons (☀️/⚙️/🛢️); `WorstPointSection` (mode toggle, setpoint slider, observed-Δp Sparkline trace vs setpoint marker, pump P_el); `HeatingCurveSection` (3G/4G presets + editable params); `WeatherSection` (−30…45 °C knob → debounced `PUT /weather/override`, badge, release); `NetzStudio` tab (catalog | policy | KPI tiles + load-duration Sparkline vs design marker + apply → Live remount via liveKey); MenuBar `Datei` (Szenario speichern…/laden/löschen); `Sparkline` port + 7 unit tests (marker extends the axis, never the data); `GET /network` rebuilt per request (CRUD changes the inventory). i18n DE/EN complete.
- **Tests: 104 backend** (82 kept green + 22 M4; surface test re-pinned to the 41-route M4 inventory, API 0.4.0, regenerated docs/API.md) **+ 20 UI vitest** (13 + 7 Sparkline); `npm run build` green (tsc strict).
- **Acceptance evidence (§12 M4):**
  1. *3G/4G story* (appendix_a, t_amb 0 °C): 3G 110/70 → t_flow 95.0 °C, losses **31.10 kW (16.3 %)**, mdot 1.152 kg/s; 4G 70/65 → t_flow 68.1 °C, losses **21.35 kW (11.8 %)**, mdot **2.199 kg/s** — losses −31 %, mass flow ×1.91 (and pump P_el 0.34→0.64 kW: low-ΔT syndrome), both directions asserted.
  2. *Δp convergence*: from plift 2.0 (worst point 1.953 bar) to setpoint 0.7: strictly monotonic 0.05 bar/tick descent, ±0.02 bar after **26 ticks**, settles 0.703; pump P_el 0.39→0.14 kW. No inner loop; blindness test green.
  3. *Scenario round-trip*: equipment (hx+storage+bypass+consumer) + 3G curve + Δp controlled 0.8 + override −5 °C + clock (step 300, 5 s) → save → heavy mutation incl. full network swap → load → everything restored incl. clock ±2 ticks.
  4. *Storage*: SoC bookkeeping exact (delivered = η·ΔSoC, round trip η²), power/capacity limits enforced (full store self-idles, near-empty discharge decays geometrically then idles), converged through every transition.
- **Verified end-to-end** (uvicorn + Vite, browser tooling; hidden-tab caveat handled via DOM/synthetic events since canvas screenshots time out): right-click ElementMenu on the plant → storage placed (🛢️ appears, `/storages` confirms) → marker menu "Speicher laden" → SoC 0→7.1 kWh live at 50 kW; NetzStudio Demo-Dorf + 4G preset preview (222 kW design, LHD 0.46 **below** the viability rule — warning shown) → apply → Live remount with supply colorbar re-anchored at 70 °C, losses 22.6→15.6 % visible; weather knob −12 °C → demand 73.9→116.8 kW, release restores; Datei-menu save ("UI Test" via prompt) + reference-scenario load → 3G recipe live (curve 110, clock 07:0x) and the Δp controller **visibly tracked the morning ramp** (plift 2.5→1.45→3.65 bar) settling at observed 0.710 vs 0.7 setpoint; pinned plant section streams boiler metrics (fuel 193.7 kW = q/0.95); DE/EN flips all new components.
- **Discoveries:**
  1. **`circ_pump_const_mass_flow` fixes the pressure at its flow junction for types containing "p"** (`CirculationPump.create_pit_node_entries` → `set_fixed_node_entries(..., 'p')`). A second pressure-fixed node in a slack loop **over-determines the hydraulics — all retry tiers fail** (verified on appendix_a; the platform behaves per §3.3: failed frames, loop alive, removal recovers). The pressure-free **`type="t"` + `p_flow_bar=None`** variant converges and coexists with the slack — the storage discharge branch **requires** it, and live `pump_mass` placement defaults to it (`p_flow_bar` optional → expert "pt" booster). File-defined pump_mass producers keep the M1 contract.
  2. heat_consumer rows **can switch setpoint pairs live** (treturn↔mdot via NaN swaps) — the storage idle-floor policy and §3.2 bypass floor rely on it; pandapipes re-derives the control mode per pipeflow call.
  3. Dropped element indices are **reused** by the next create (`_get_index_with_check`) — CRUD bookkeeping must key on current tables, never on creation history.
- **Deviations (documented, deliberate):** live pump_mass placement requires only `mdot+t_flow_k` (`p_flow_bar` optional, see discovery 1) vs the M1 file contract's all-three; demo_dorf's honest annual-energy LHD is 0.386–0.46 MWh/(m·a) (the M3 log's ≈0.9 used a different basis) — kept, it's the teaching point of the NetzStudio viability marker; storage SoC starts at 0 on scenario load (recipes, not snapshots); `POST /networks/import` deferred to M6.
- Next: **M5 — measurement layer** (sensor placement CRUD, `key_points` preset, fidelity modes full/standard 15-min windows, strict-mode + blindness + cold-start tests).

### 2026-07-16 — M5: measurement layer (branch `m5-measurement-layer`)

**Built** (SPEC §12 M5; §7 Sensors row/§8/§8a/§9.2/§11):
- `sensors.py` — real placement model replacing the M2 interim: `consumer_meters: set[element]` (Wärmemengenzähler: q/mdot/T_s/T_r/dp) + `node_sensors: set[node]` (T/p pair: supply+return p and T at the node's junction pair) + plant SCADA (always measured, never appears as a placement, never degrades — it is telemetry, not metering). Fidelity `full`/`standard` as ONE bulk mode (SPEC §7 has a single endpoint; the blueprint's per-device TAF overrides deliberately not ported): standard = 15-min-window means **aligned to simulated time** (`window w = global_tick // window_steps`, steps_per_day-aware, survives day wraps), blueprint accumulator pattern (`_win/_acc/_held`, boundary publishes means), channels null until the first boundary after placement/switch — honest cold start, a device placed mid-window publishes its first (partial) mean at the next boundary (blueprint TAF-7 parity). Presets REPLACE the placement: `all_consumers` (default — M2–M4 parity, applied through the placement machinery on every network load/swap) · `plant_only` (plant T/p pair — now genuinely distinct from `clear`) · `key_points` (plant T/p + net-end leaves T/p + heat meter at the *currently known* worst point from the last converged frame; pre-solve: no meter, honest) · `clear`. Placement mutators flip `preset` to `"custom"`; removal drops the device's window state; `remove_consumer` prunes its meter. Coverage fractions per element class in `placement()`.
- **Δp blindness semantics (the documented M5 decision):** the controller keeps consuming ONLY `observed_summary.dp_worst_bar` = min over *metered* consumers. Degradation ladder: (1) no usable Δp reading (clear/plant_only, or every meter cold-starting in standard mode) → `None` → **hold** `plift` — holding *is* "controlling on plant Δp" (SPEC §8a): a constant-Δp pump with no remote sensor; (2) metered but the TRUE worst point unmetered → regulate on the best *measured* Δp (what a real Schlechtpunkt controller does) and flag `controls.dp_control.blind_spot` (UI-visible). The flag is deliberate meta-information about sensor-layout adequacy (SPEC §8a: "the UI shows why that's worse") — it names no physics value and stays visible in strict mode.
- API — `api/measurements.py` per §7: `GET /measurements` (placement + coverage + `expose_ground_truth` for the panel's strict notice), `POST/DELETE /measurements/consumer/{id}`, `POST/DELETE /measurements/node/{id}`, `POST /measurements/mode {full|standard}`, `POST /measurements/preset` — every verb returns the fresh placement; 404 unknown element/no device, 422 bad mode/preset (Literal). 48 routes, `API_VERSION 0.5.0`, docs/API.md regenerated, surface test re-pinned.
- Scenarios — recipes now carry the sensor layout: meters saved by consumer NAME (element ids shift; the op replay recreates names deterministically), node sensors by node name, plus mode + preset label; replay after the consumer ops (tolerant per entry), M4-era preset-only docs still load (preset re-applied against the fresh context). References updated: 3G Winter `all_consumers`+`standard`, 4G Vergleich `all_consumers`+`full`.
- UI — `MeasurementPanel` port (§9.2): full/standard segment, coverage bars 📟 meters + 🌡️ sensors, four preset buttons, strict-mode notice; `ElementMenu` gains place/remove heat meter (consumers) and T/p sensor (nodes + plant); `MapDiagram` gains decorative 📟/🌡️ markers diffed against the placement and honest standard-mode states (cold start: UNOBSERVED fill + dashed ring + popup "warte auf das erste 15-min-Fenster"; populated: values + "15-min-Mittelwerte" hint — live popups refresh through both transitions); `WorstPointSection` shows the blind-spot note (two texts: no reading at all vs true worst point unmetered); `LiveHeatFlow` re-syncs the placement from every measurement verb and after equipment CRUD. DE/EN complete (tsc-enforced via `typeof de`).
- **Tests: 120 backend** (104 kept green + 16 M5) **+ 20 UI vitest**; `tsc && vite build` green.
- **Acceptance evidence (§12 M5):**
  1. *Blindness*: (a) preset `clear` + setpoint above the truth worst point → plift held exactly over 6 ticks despite the true violation, observed Δp `None`, `blind_spot` true; (b) meter ONLY at the max-Δp consumer → controller drives THAT reading to 0.7±0.02 (asserted == that consumer's truth channel) while the true worst point ends < setpoint — acted on measured, never truth; honesty tripwire: consumer A's 70 °C return anomaly visible in truth, absent from every meter entry and aggregate (`n_metered`/`q_demand_metered` exclude A).
  2. *Strict mode*: `EXPOSE_GROUND_TRUTH=false` + partial placement (1 meter + 1 node sensor) → `/state`, `/history`, WS frames carry no truth keys, blank error, and a fully functional measured projection (meter channels, node p/T, plant SCADA, observed_summary).
  3. *Cold start*: boundary tick exact — ticks 0..14 all channels null (observed Δp null → controller blind), tick 15 publishes the mean over 0..14 (assert mean ≠ instant via an in-window ramp), ticks 16..29 hold it, tick 30 rolls the window; mid-window placement stays null until the NEXT boundary then means over the ticks it saw; mode switches reset window state both ways.
- **Verified end-to-end** (uvicorn + Vite, browser tooling; hidden-tab caveat → Leaflet internals reached via React-fiber hook walk, style-level assertions instead of canvas pixels, synthetic `contextmenu` with a real `MouseEvent`): measured view with `clear` → all 11 demo_dorf consumers flip to `#39424f` + all 14 trenches dashed, panel 0/11 + 0/15 + "keine Messstellen", WorstPoint "blind — kein Messwert"; right-click consumer on the map → "📟 Wärmemengenzähler setzen" → coverage bar 1/11, marker recolored, 📟 icon appears; standard mode (paused-engine window reset) → all five channels null mid-window with dashed/muted marker + cold-start popup, after the boundary the meter shows q 2.04 kW vs truth instant 2.95 kW (a mean, visibly) + "15-min-Mittelwerte" hint; `key_points` → meter at C5 EFH (= truth worst) + T/p at plant + 4 net ends (5/15), node readings live; **the blind-spot flag tracked the truth worst point oscillating C9↔C5 under DHW jitter** — flag true exactly while the unmetered C9 was critical (observed layer honestly reporting metered C5 at a HIGHER Δp), the §1 moving-worst-point lesson live on screen; DE/EN flips the panel.
- **Discoveries:**
  1. The embedded browser pane's hidden tab intermittently hits `connect ETIMEDOUT 127.0.0.1:8000` on the Vite proxy (backend healthy, curl fine, `/status` polls fine) — a failed fetch triggers the app's `window.alert`, which **wedges the whole pane** (every CDP JS call times out; only a fresh tab recovers). For future agents: stub `window.alert` before UI E2E and keep control verbs on curl.
  2. App.tsx never cleared `topoErr` after a successful `/network` refetch — a startup proxy flake left a permanent error banner over a working app. Fixed (one line) in this branch.
- **Deviations (documented, deliberate):** fidelity mode is set-global, not per-device (SPEC §7 defines one bulk endpoint; blueprint per-device TAF overrides add API surface M5 doesn't need); a mid-window-placed standard meter publishes a partial first window at the boundary (blueprint parity) rather than waiting a full window; `blind_spot` is served even in strict mode (meta-information about the sensor layout, required by §8a's "show why it's worse" — not a physics leak); presets replace the placement wholesale (bulk actions, not increments).
- Next: **M6 — ops** (recording→CSV, bulk exporter, InfluxDB/Grafana, Docker Compose, `.bat` launcher, CI→GHCR, `POST /networks/import`).

### 2026-07-16 — M6: ops (branch `m6-ops`)

**Built** (SPEC §12 M6; §4.6/§6/§7/§9.3):
- `recorder.py` — blueprint port: recording as a **publish sink** on the M2 `StateStore.sink` hook, wired in the API lifespan as `store.sink = lambda r: recorder.record(store.frame(r))` — the recorder consumes the **projected** frame (our sink hook passes the raw `StepResult`, unlike the blueprint's pre-projected stream), so strict mode gates the CSVs too (pinned: no truth CSV exists under `EXPOSE_GROUND_TRUTH=false`). Dedicated writer thread + queue (`record()` = `queue.put`, engine never blocks — pinned: 40 frames through a 50 ms/frame-slow writer publish in <0.8 s and drain fully on `stop()`). One recording per configuration: consecutive `(day, step)` duplicates dropped, backward seeks kept (wall-clock `timestamp` column disambiguates). Pack layout `data/recordings/<id>/`: `summary.csv` (physics aggregates + `converged`/`solver_status`/`solve_ms`), `observed_summary.csv`, `junctions/pipes/consumers/producers/storages.csv` (fixed column plans mirroring the §6 wire lists), `controls.csv` (dot-flattened heating-curve/Δp settings + weather), `measurements_consumers/nodes/plant.csv`, `metadata.json` = reproducibility recipe (`recording_meta()`: version, `active` network+loadgen, sensor placement+mode, heating-curve/Δp/plant config, engine clock, `expose_ground_truth`). Files appear lazily on first row; dict-valued files pin their column set from the first row (a mid-recording config change can never shift columns). ZIP download cached (`<id>.zip`, invalidated via metadata mtime); path-traversal-guarded ids.
- `exporter.py` — `BulkExporter`: `/export/days` briefly parks the engine, deep-copies the live Simulator off-loop, resumes, then replays the requested days through `run_step` back-to-back in a worker thread into a private `Recorder`, projecting every frame through a fresh `StateStore.frame` (strict-safe). **`prepare_replay(sim, first_day)`** (public — the byte-compat test uses it) normalizes to a deterministic from-midnight state, resetting exactly the run-state a scenario load discards: plant `t_flow_k` evaluated for the first replayed tick, cold init (§3.4), storages to SoC 0 (M4 convention), fresh measurement windows, cleared last-payload/blind-spot, and a **controlled** Δp pump released to its file-defined lift (run-state, like the blueprint's released controllers) while a **fixed** pump keeps the user's plift (config). Progress `steps_done/total/ETA`, cancel between steps (partial pack finalized + `export.cancelled` marked), one at a time (409). Quasi-static only — the §3.5 transient flag stays M7 (exporter-only there too).
- API — `api/recordings.py` per §7: `GET /recording`, `POST /recording/start|stop`, `GET /recordings`, `GET /recordings/{rid}/download` (ZIP; 409 while being written), `DELETE /recordings/{rid}`, `POST /export/days {days: n|[indices], name?}`, `GET /export`, `POST /export/cancel`. Recording **auto-stops** on `/config/apply` and scenario load (hook in the shared `apply_network`); `RTHEATFLOW_RECORD=true` = continuous operation, one pack per configuration (start at startup, rotate after apply/scenario load, finish on shutdown — pinned). **58 routes, `API_VERSION 0.6.0`**, docs/API.md regenerated, surface test re-pinned.
- `POST /networks/import` (deferred from M4) — five-file JSON bundle (`{name?, network_structure, pipes, consumers, producers, weather}`) → written to `data/user_networks/<slug>/` (collision → `-2` suffix, never overwrites), validated by **actually loading** the full contract; invalid bundles get the directory removed + 400 (blueprint convention; pinned incl. missing-slack and unknown-node cases). `NetworkCatalog` gains the `user_networks` scan (`user_<dirname>`, `source="user"`, cheap list stats from two JSON reads, lazy rescan when an unknown `user_*` id is looked up) — imported networks appear in `GET /networks`, preview, apply, NetzStudio.
- UI — MenuBar Datei grows the blueprint recording workflow: ⏺ Aufzeichnung starten/beenden with live step count, Aufzeichnungen flyout (💾 ZIP download links + delete), „Tage exportieren…" dialog (1–366), export progress row with ETA + cancel; activity chips (`REC n` / `Export p %`) visible with all menus closed (3 s global poll, 2 s while the menu is open) and jumping into the Datei menu on click. NetzStudio column 1 gains „Netz importieren…" (file picker: the five contract `.json` files together or one bundle file → `POST /networks/import`, list refresh + auto-select) and a separate „Eigene Netze" section. i18n `rec.*`/`netz.import*` DE/EN complete; `tsc && vite build` + 20 vitest green.
- `visualization/` — `collector/collector.py` (+ python:3.11-slim Dockerfile): polls `GET /state` at `POLL_INTERVAL_SECONDS` (0.5 s), **dedupes on `(day, step)`**, writes wall-clock-timestamped Influx points: `summary` (all numeric KPIs + `converged` + `solver_state` 2/1/0 + `dp_setpoint_bar`/`dp_plift_bar` from controls + weather), `junction`, `pipe`, `consumer`, `producer`, `storage`. Grafana 11 fully file-provisioned (datasource + dashboard JSON in-repo): plant supply/return vs setpoint, worst-point Δp vs setpoint + pump lift (0.5 bar threshold line), producer dispatch stack, storage SoC, loss-ratio gauge, solve-time stat, solver-status mapping (OK/DEGRADED/FAILED).
- Docker — backend `python:3.11-slim` (PYTHONPATH=/app/src, committed `data/` baked in → runs standalone, EXPOSE 8000, `RTHEATFLOW_HOST=0.0.0.0`); ui two-stage node:22-alpine→nginx:1.27-alpine with SPA fallback + `/api/` trailing-slash proxy strip + `/ws` upgrade headers + 1 h read timeout (§9.3); `docker-compose.yml` with the 5 services (backend :8000 + `./data` volume, ui :8080, influxdb 2.7 with init env + healthcheck, collector `depends_on` influx-healthy, grafana :3000 provisioned), named volumes, `restart: unless-stopped`, dev-credential warnings. `.dockerignore` root **and `ui/`** (the ui context would otherwise bake the host's Windows `node_modules` — platform-broken esbuild — into the linux image).
- `start_rtheatflow.bat` — blueprint launcher semantics (German comments): port-listen guards 8000/5173, `.venv` check with printed setup instructions, auto `npm install` when `ui\node_modules` missing, backend + Vite in separate consoles, `/health` poll ≤60×1 s (numba cold start), browser open.
- CI `.github/workflows/docker.yml` — job `test` (Python 3.11, `pip install -r requirements.txt -e .[dev]`, `pytest -q`) gates job `build`: 3-image buildx matrix (`backend` ctx `.`, `ui` ctx `ui`, `collector` ctx `visualization/collector`) → `ghcr.io/<owner>/rtheatflow/<image>`, tags branch/pr/semver/sha + latest on default, per-image GHA cache, concurrency cancel-in-progress, push skipped on PRs.
- **Tests: 134 backend** (120 kept green + 14 M6: 11 recording/export + 3 import) **+ 20 UI vitest**; `tsc && vite build` green.
- **Acceptance evidence (§12 M6):**
  1. *Live vs export byte-compatibility* (the acceptance bar): one shortened day (24 steps) recorded live through the publish path, then bulk-exported from a deep copy of the **drifted** simulator (warm-start advanced, Δp pump moved, windows filled) — every CSV **byte-identical** after masking exactly two documented volatile columns: `timestamp` (wall-clock) and `solve_ms` (machine timing); metadata recipes equal modulo the documented bookkeeping keys (`id`, `started`, `ended`, `files`, exporter `export` block). Parametrized over **appendix_a with the Δp controller actively regulating** (plift moving every tick) and **demo_dorf with the heating curve on the weather day** — both green because `prepare_replay` restores the exact normalized start state (same columns, same `_r()` rounding, same row order).
  2. *Recording never blocks*: slow-sink stub (50 ms/frame) → 40 publishes in <0.8 s, full drain on stop; dedupe + backward-seek semantics pinned; auto-stop on apply/scenario load pinned (2 finished packs on disk).
  3. *Import round-trip*: demo_dorf's five files → `POST /networks/import` → `user_dorf-kopie` in the catalog (`source="user"`) → preview → `/config/apply` → engine solves converged frames on the import. Collision + invalid-bundle rejection (files removed, catalog clean) pinned.
  4. *Compose up works* (Docker Desktop 29.5.3 on this machine): 3 images build; `docker compose up -d` → all 5 services up, influx healthy; smoke-verified end-to-end: `/health` direct **and via nginx** `http://localhost:8080/api/health` (prefix strip), SPA 200, `/state` via nginx streaming demo_dorf frames (converged, ~18 ms solves), **collector writing deduped per-step points** (Flux count query against `heatflow` bucket), Grafana datasource health "3 buckets found" + dashboard `rtheatflow-heatflow` provisioned, nginx `/ws` upgrade → `101 Switching Protocols`. Torn down with `-v`.
  5. *Launcher verified end-to-end on this Windows 11 machine*: both consoles spawned, health poll passed (no WARNUNG), Vite `:5173` serving with working `/api` dev proxy, browser opened; spawned consoles killed afterwards, ports verified free. (Port 5173 was initially occupied by an orphaned Vite server from a stale, git-pruned agent worktree — killed after verifying the owning command line.)
  6. *CI runs on the branch push*: the first-ever Actions run (dc64163) executed and **caught a real latent bug** — see discovery 1; fix pushed (3504d43), run green (`test` 134 passed → 3-image build+push to GHCR).
- **Discoveries:**
  1. **`engine.reconfigure`'s M2-era `pause()` raced the in-flight step**: `pause()` only takes effect at the next loop iteration, so the current `to_thread(run_step)` of the OLD network could publish *after* `store.reset()` on slow hardware (2-vCPU CI runner), briefly serving a stale 3-consumer appendix_a `/state` for demo_dorf. Passed locally for three milestones; the first CI run failed `test_config_apply_swaps_the_network`. Fixed to blueprint semantics: reconfigure **awaits `stop()`** (drains the in-flight step) before swapping, restarts the loop task afterwards.
  2. PS 5.1 `Invoke-WebRequest` on this machine routes `localhost` through the configured system proxy — compose smoke checks time out misleadingly; `curl.exe --noproxy "*"` is the reliable probe (future agents take note).
  3. The blueprint's exporter feeds `asdict(run_step(...))` **unprojected** into its recorder — in strict mode a netzsim export would leak truth the live recording withholds. Ours projects export frames through `StateStore.frame`, so live-vs-export byte-compatibility holds in strict mode too (deliberate improvement, documented here rather than silently diverging).
- **Deviations (documented, deliberate):** CSV pack columns are DH-native (per-file fixed plans + `solver_status` next to `converged` in `summary.csv`) — "byte-compatible" is live-vs-export within rtheatflow, not netzsim file parity; `controls.csv` carries the flattened controls **and** weather (one operator-settings row instead of a sixth dict file); `prepare_replay` releases a controlled pump to the file lift but keeps a fixed pump's plift (run-state vs config split — scenario docs save plift, so fixed-mode plift is config); `POST /export/days` has no `estimate` flag (estimation is M7; the blueprint's flag guards WLS runtime); CI triggers on **all** branch pushes (blueprint: master only) so milestone branches build branch-tagged images and the M6 verification item "CI runs on first push" is real; UI recording start sends no name (blueprint parity — the API accepts one).
- Next: **M7 — estimation layer + polish** (forward-simulation observer, est view, honesty tripwires; optional transient exporter flag §3.5; ARCHITECTURE.md, Benutzerhandbuch).

### 2026-07-16 — M7: estimation layer + polish (branch `m7-estimation-polish`) — FINAL MILESTONE

**Built** (SPEC §12 M7; §1/§3.5/§6/§8/§8a/§11/§2):
- `estimator.py` — **ForwardObserver** (§8a: pandapipes has no WLS; the estimated layer is a forward-simulation observer): a deep-copied twin net rebuilt on a topology/policy **signature** (consumer ids+names, hx/pm/storage elements, junction/pipe counts, prior basis, weather coupling params), per estimate driven ONLY by operator knowledge — plant SCADA `t_flow` (from `measurements.plant`) + the operator's `plift` (controls), metered consumers' channels **fidelity-respecting** (standard-mode window means; cold-start `None` → prior per channel; treturn rows take measured q+T_r, deltat rows take q + measured (T_s−T_r), mdot rows take q+mdot), **pseudo-priors** for unmetered consumers, operator equipment dispatch copied from live INPUT columns (hx qext, pump_mass/storage-discharge mdot/t_flow/in_service, storage-charge rows — config, always visible per §6), ground temp + **true ambient incl. override** for the §4.5 prior scaling (documented decision: the plant has a weather station and the override is the operator's own knob). Twin solves with the same retry ladder; converged → twin warm-starts from ITSELF (never inherits the truth's junction state — reset to supply-temp init at rebuild); failure → stale estimate, never a crash.
- **PriorBook (documented prior-basis decisions):** priors read `sim.inputs` (the immutable planning contract) + the archetype cache + placement recipes — **never** `sim.profiles`/net tables (the mutable runtime truth), so anomaly injection cannot leak. Hierarchy: (1) runtime recipes — archetype recipe → expected archetype profile at the recipe's `day_percentile` (scale honored, no jitter/seed/variant knowledge), `q_kw` recipe → the flat plan, bypass → the configured pair; (2) file consumers with a `building` tag → archetype expected profile: **deterministic VDI SH day-matched to the weather** (expected day energy `q_design_arch · mean-daily f(T_amb) · 24 h` → closest archetype-year day; a degree-hour load forecast) + **mean DHW across the stochastic variants** (diurnal mean shape across the year), scaled by contracted `q_design_file/q_design_arch`; (3) untagged file consumers → the hand-authored plan curves (teaching nets carry no stochastic realization worth hiding); basis `design` (config) forces the crude `q_design·f(T_amb)`, no DHW. treturn/deltat/mdot partners always from planning values. Per tick the SH prior runs through the SAME §4.5 override scaling as the truth.
- **Throttling (blueprint pattern):** two gates ANDed — metering **raster** (mode `standard` + any placed device → refresh only at 15-min window boundaries; no new information exists in between) and **wall-clock self-throttle** (`throttle_factor` × the estimation's own `solve_ms`, default 2.0); the last estimate attaches to every following frame (failed truth frames too — consistent with the reused-last-converged policy) honestly stamped `step/day/seq`; `_wall` stamps even on failure (no retry storm against a broken twin).
- **`estimated` payload** (§6): junctions/pipes/consumers/summary **mirroring the truth shape exactly** — `_collect`'s physics block extracted into module-level `collect_physics()` (simulator.py) shared verbatim by truth and twin (one direction-aware loss formula, one `mdot·c̄p·ΔT` feed) — + `error` + `step/day/seq/solve_ms/solver_status`. **`error` = the observer's innovation**: |twin − measurement| at sensored points ONLY (meters: T_return/mdot/dp; node sensors: T_return + pair Δp; plant SCADA: T_return/mdot), fields `max/mean_dt_return_k`, `max/mean_dmdot_kg_per_s`, `max/mean_ddp_bar`, `n_points` — measurement-based (deliberately not the blueprint's truth-based error), hence **valid and visible in strict mode**; estimated survives `_project()` (nested keys; derived from measurements — blueprint semantics).
- API — `GET/POST /estimation/config` (partial update: `enabled` default on · `prior_basis archetype|design` · `throttle_factor` 0–20, 422 bounds; response carries `seq`/`last_solve_ms`), policy held on the **engine** (`set_est_config`) and re-applied across `reconfigure` → survives `/config/apply` and scenario loads. `GET /manual` (see docs). **61 routes, API 0.7.0**, docs/API.md regenerated, surface re-pinned; `recording_meta` gains the estimation policy.
- **Transient flag (§3.5, exporter-only):** `solve_with_retry(transient_ctx={"dt", "step"})` prepends two transient bidirectional tiers (always explicit `dt` — bug #787; monotonic `simulation_time_step`, 0 = cold chain start) before the quasi-static ladder = automatic per-step fallback; `run_step(solve_ctx=...)` (live engine never passes it) + `sim.last_transient`; `BulkExporter` replays transient when `RTHEATFLOW_TRANSIENT=true` and marks the pack (`export.transient`, `transient_fallback`, `transient_fallback_steps`). **Chaining proven before use** (the task's precondition): per-step standalone `pipeflow(transient=True, dt, simulation_time_step=k)` reproduces `run_timeseries(mode="bidirectional", transient=True, dt=60, iter=100)` with a ConstControl demand ramp **bit-identically (max |Δ| = 0.0** for t_k and p_bar over 6 steps on the Appendix A build) — expected, since `run_timeseries` internally does exactly this (`run_time_series.py` injects `simulation_time_step=i` per step; `initialize_pit` reuses `net["_pit"]` and snapshots TINIT/TOUTINIT into the old pit when `transient ∧ step≠0 ∧ net.converged`). Inertia visibly engages (far-end supply 358.05→357.57 K decaying vs the quasi-static 351.8 K jump — >1 K pinned; the §3.5 transport-delay story).
- UI — the **Schätzung** segment live (three-layer switcher complete): §8 fallback chain (`truth→observed` strict; `est→truth/observed` when no estimate) + the blueprint **splice pattern** (`frame = {...latest, junctions/pipes/consumers/summary: est.*}` → the SAME MapDiagram/OverviewSection render it); OverviewSection est caption + **quality badge** (innovation max |ΔT_r|/|Δṁ|/|ΔΔp|, estimate age from the stale-attachment stamps in simulated minutes + `#seq`, observer solve time, honesty note); Hilfe menu → `/api/manual`; `EstimatedState`/`EstimationConfigInfo` types, api.ts wrappers; i18n DE/EN complete. `tsc && vite build` + 20 vitest green.
- Docs — **`docs/ARCHITECTURE.md`** (Engine/Simulator/Store, contract, solver policy incl. retry ladder + warm start, controllers-on-observed, all three observability layers incl. the observer design, recording/export, API/frontend/ops, and a **prominent section 4 "Physical fidelity"**: quasi-static default and what it physically means — no temperature-front transport delay in the live loop — upstream transient status #534/#787, our proven exporter-only stance, `sections ≈ v·dt` guidance); **`docs/Benutzerhandbuch.md`** (German, 14 sections: Schnellstart Launcher/Compose, Arbeitsablauf Netz–Lasten–Live, die drei Sichten als Lehrtabelle, Karte/Ebenen, Ausrüstung, Messstellen & Abdeckung, Heizkurve & Schlechtpunkt mit Blindflug, Wetter-Regler, die Schätzung im Detail (Schätzgüte = Innovation, Prior-Basis, Ehrlichkeitsregeln), Szenarien, Aufzeichnung & Export incl. Transient-Hinweis, Grafana, **„Grenzen des Modells — bitte lesen" (quasistatisch prominent)**, Konfigurationstabelle) served at **`GET /manual`** as themed HTML via an in-house ~120-line markdown-subset renderer in `api/core.py` (headings/lists/tables/fenced code/inline/blockquote/hr, HTML-escaped; `?format=md` raw; no third-party markdown dependency for one static document — pinned incl. injection-escape test); README → M7/complete; Dockerfile bakes `docs/`; `.env.example` transient text updated.
- **Tests: 154 backend** (134 kept green + 13 estimation + 5 transient + 2 manual) **+ 20 UI vitest**; `tsc && vite build` green.
- **Acceptance evidence (§12 M7):**
  1. *Honesty tripwires*: (a) doubled demand + 15 K return anomaly injected on UNMETERED consumer A (runtime profile arrays) → truth shows 160 kW/70 °C, estimate stays at the prior (80 kW/55 °C plan, asserted <90 kW & <57 °C); the SAME anomaly with the meter ON A propagates (>155 kW, >68 °C). (b) `clear` preset → every estimated consumer q/treturn equals the prior exactly (±1e-3 kW/±0.05 K — the twin is DRIVEN by them) while the priors provably differ from the per-tick truth. (c) coverage degradation monotone at demo_dorf 08:00: plant-mdot innovation 0.000 (all_consumers) < half coverage < 0.189 kg/s (clear); the reported `error.max_dmdot` carries the same signal.
  2. *Estimate tracks reality where measured*: all_consumers full fidelity → est summary vs truth within 0.1 kW (feed), 0.01 kW (demand), 1e-3 kg/s (mdot), 1e-3 bar (dp_worst); error metric ≈ 0 (max ΔT_r < 0.01 K) — full observability = exact reconstruction (the twin is pinned by measured boundary conditions everywhere). Partial coverage → error > 0 and equals the real deviation at the remaining sensors.
  3. *Transient flag*: default off pinned; off ⇒ M6-identical (byte-compat suite unchanged & green with the observer live — the recorder's fixed column plans ignore `estimated`); on ⇒ working transient replay (24-step day on appendix_a: pack completes, `transient_fallback=false`, junction temps differ >0.5 °C from the quasi-static pack) with the chaining-proof regression; sabotaged transient tiers (monkeypatched pipeflow) → 24/24 steps fall back quasi-static, all converged, marker set, never a crash.
  4. All prior suites green; `tsc && vite build` + vitest green; E2E est-view check via browser tooling (below).
  5. `/manual` serves the Benutzerhandbuch (HTML with the Grenzen-des-Modells section + tables; `?format=md` raw; 422 on unknown format).
- **Discoveries:**
  1. **Transient chaining is literally what `run_timeseries` does** — `run_loop` injects `kwargs["simulation_time_step"] = i` per step and calls plain `pipeflow`; the chained state is `net["_pit"]` + the old-pit TINIT/TOUTINIT snapshot taken in `initialize_pit` (before component entries when `transient ∧ step≠0 ∧ converged`). Nothing else carries state → standalone per-step calls with the same kwargs are the same mechanism (proof test pins bit-equality). `pipeflow` re-runs `create_lookups` every call, so in_service flips (storage discharge) stay safe under pit reuse.
  2. With FULL meter coverage the forward observer reconstructs the truth **exactly** (error 0.0 to the wire's 6 digits) — qext+treturn boundary conditions at every consumer + measured plant t_flow pin the whole coupled solution; deviations exist only where priors substitute for sensors. Makes "coverage buys certainty" quantitative on screen.
  3. pandapower's `OutputWriter` default `log_variables` references `res_bus` and crashes on pandapipes nets — always pass `log_variables=[("res_junction", ...)]` explicitly (matters for any future run_timeseries use).
- **Deviations (documented, deliberate):** `error` is measurement-based innovation, not the blueprint's truth-based estimate error (strict-mode validity + §8a "deviation from measurements quantifies estimate quality"; with full coverage both read ~0); estimated attaches to failed frames too (blueprint leaves it absent — ours matches the platform's reuse-last-converged failure policy); the estimated layer is **not recorded** to CSV packs and replay copies run with estimation disabled (wall-clock cadence is machine timing, not physics; recomputable from recorded measurements; keeps M6 byte-compat intact — the blueprint's `estimate` export flag equivalent is "always off"); estimation config is engine-held but NOT in scenario recipes (a view-layer policy, not part of the physical setup); `/manual` renders Markdown, not the §7 sketch's PDF (SPEC §2 names no format; the M7 brief allows rendered HTML or raw MD); per-element `/profiles` sweep routes (§7 row) were never scheduled by any milestone and remain unbuilt — the est layer rides the live frame instead (documented as the one §7 surface gap).
- **The platform is complete: all seven milestones shipped.** Final surface: 61 routes, API 0.7.0; 154 backend + 20 UI tests.

### 2026-07-16 — Sibling port scheme + stop launcher (post-M7)

- **Problem:** rtheatflow copied netzsim's ports (backend 8000, Vite 5173) — starting both platforms collided; the second Vite silently hopped ports but kept proxying `/api` into the WRONG project's backend (observed live: orphaned rtheatflow Vite on 5174 next to a netzsim-style backend on 8000).
- **Fix — sibling port scheme:** rtheatflow now owns **backend 8001 / Vite 5174** (config.py default, vite.config.ts with `strictPort: true` so Vite fails loudly instead of hopping, launcher, README, Benutzerhandbuch, gen_api_doc header). Compose host ports shifted likewise: 8001/8081/8087/3001 (netzsim compose keeps 8000/8080/8086/3000). Container-internal ports unchanged.
- **New `stop_rtheatflow.bat`:** kills the launcher consoles by window title, the owners of ports 8001/5174, and orphaned node/esbuild/python processes matched by **command line** (not exe path — Vite runs as `Program Files\nodejs\node.exe <repo>\ui\...\vite.js` and the backend as relative `.venv\Scripts\python.exe -m rtheatflow.main`; both evade exe-path filters, as do leftovers from deleted agent worktrees and servers on hopped ports). A parallel netzsim (8000/5173) is deliberately untouched.
- **Verified live:** stop script cleared a real three-process debris field (old-port backend, old-port Vite, orphaned worktree Vite on 5174) in one run; fresh start on 8001/5174 with old ports untouched; second stop → all ports free, zero leftover processes. 154 backend + 20 UI tests green; docs/API.md regenerated. Note: Vite binds IPv6 `::1` only — browsers are fine (localhost→::1 first on Windows), but IPv4 `127.0.0.1` curls against the dev UI get refused; use `http://[::1]:5174` for scripted checks.

### 2026-07-17 — Reference networks & validation (branch `reference-networks`)

**Built:** four real/published reference networks as five-file catalog entries, each converted offline-deterministically from vendored sources (`data/sources/<id>/` incl. license files, netzsim DATASET.md convention: provenance + conversion decisions + validation record per network directory).

1. **DESTEST CE1 (IBPSA, modified BSD-3)** — `scripts/convert_destest.py` → `destest_16/8/32`. **Source discrepancy discovered and resolved with evidence:** the repo's `Pipe_data.csv` and the official case-description Table 6 disagree (diameters, insulation, main lengths 36 vs 26.83 m); published CE0 pressure drops (22.4–25.4 kPa vs ~7 kPa for the CSV dims) and the CE0 i–h loss KPI (314–326 W vs 76 W) prove every published tool models **Table 6** → destest_16 built from a vendored Table 6 transcription; 8/32 from the repo CSVs (no published aggregates). The CSVs' flat "U-value 0.035 W/mK" is a design-tool constant (hard-coded in upstream `Pipedimensioning.py`) — real loss model = layered PE-X wall (0.35 W/mK, ISO 15875 S5) + insulation (0.026 W/mK) cylinder, converted per-area on the **inner** diameter (pandapipes DO fallback verified in source: `branch_w_internals_models.py`). DESTEST's 1.77 kg/h bypass ≡ **62 W at ΔT 30 K** → validation suite runs `min_qext_w=62` (floor = bypass; app default 500 W untouched). **Validation (`test_destest_validation.py`, reference values recomputed at test time from vendored result CSVs; bands = published spread + argued fluid/quasi-static margins):** CE0 mdot 8876.5 kg/h (pub 8848–8870), t_ret 39.48 °C (39.46–39.85 ✓ inside), heat 314.8 kW (308.2–314.3), supply@SD1 69.454 °C (69.43–69.48 ✓ inside — the loss physics pin). CE1 7-day (10-min grid = source resolution, 1008 tier-1 solves ~30 s): injection 14.506 MWh (pub 14.32–14.45), losses 606.6 kWh (pub 535–544) — **loaded-tick subtotal 534 kWh inside the published band**; the +72.5 kWh excess sits entirely in the 401 near-zero-flow night ticks (quasi-static steady state keeps pipe entries hot where dynamic tools cool down; bounded by the 134 kWh bypass-enthalpy ceiling — derived, not tuned). DIMOSIM excluded (published outlier, 11.5 MWh).
2. **Schutterwald (pandapipes BSD-3)** — `scripts/convert_schutterwald.py` → `schutterwald`, the real-town map flagship. Verified source traps: `geo` columns store **[lat,lon] reversed vs GeoJSON** (= our native order, taken as-is), `junction_geodata` EPSG:31467, all diameters 800 mm + u=10 (dead columns), **34 zero-length pipes/side**, supply/return a perfect `return_<name>` mirror. Conversion: supply side only; zero-length pipes + the 2 open DN200 feeder valves merged via union-find (244→208 trench nodes, 207 trenches, 2.626 km); DNs re-sized from downstream design loads (ΔT 40 K, ≤1.2 m/s, ISOPLUS_DRE*_STD → DN20…DN125, 1904 kW design); loads = 1506 gas-net houses → nearest substation in EPSG:31467 with a **documented 50 m service-area cutoff** (all-houses = 33.4 GWh/a = LHD 12.7 on 2.6 km would be physically incoherent; 50 m ⇒ 155 houses, 3.52 GWh/a, LHD 1.34), archetype profiles scaled to gas-derived annuals (spread 9.5–214 MWh/a), 90th-percentile winter day, 3G preset. Physics: 96/96 steps tier 1 (~60–220 ms), balance ≤0.075 %, v_max 0.98 m/s, day loss ratio 7.5 %, annualized ≈13 % (the 8–20 % plausibility band applies to the annual ratio — winter-instantaneous is naturally lower, documented).
3. **Verbier (OpenDHN, CC BY 4.0, Zenodo 10.5281/zenodo.10793816)** — `scripts/convert_verbier.py` → `verbier`, real **measured meshed** net (AGPL pydhn deliberately not consulted). 676 trench nodes / 681 trenches (12.05 km, **6 loops**), supply/return mirror verified lossless, 150 substations with measured CASE1 power + return temps (qext+treturn), layered insulation U per pipe, anonymised metres → synthetic Lake-Geneva anchor. **Contract extension (documented):** `p_flow_bar` now optional for *file-defined* `pump_mass` → pressure-free `type="t"` (mirrors the M4 live rule; no existing file used pump_mass, so no behavior change) — required for the second plant HS1 (5.03 kg/s, 81.81 °C) next to the HS0 slack. **Validation vs monitoring data:** total feed 3567 vs 3590 kW measured (−0.6 %), HS0 mdot 17.83 vs 17.76 kg/s, HS0 return 44.10 vs 43.52 °C, losses 297 vs ~317 kW implied; **150 substation supply temps: median |ΔT| 0.83 K, p90 2.68 K, max 12.6 K** (outliers named + analyzed in DATASET.md); substation flows median 1.9 %. **Solver: tier 2 (α=0.5) converges, tier 1 fails** — the expected meshed stress case; cold ~28 s, warm ~5 s/solve (live ticks lag at this size — documented).
4. Cross-cutting: catalog entries (`benchmark`/`rural`/`meshed` characters), README "Reference networks & validation" (live-verified numbers only), Benutzerhandbuch §15 (Referenznetze), this entry.
- **Tests: 188 backend** (154 kept green + 20 DESTEST + 6 Schutterwald + 8 Verbier) **+ 20 UI vitest**; UI untouched (`heating_curve: null` for constant-supply nets was already modeled — note: the supply color ramp anchors at 110 °C when no curve exists, so 70 °C DESTEST renders mid-ramp; cosmetic).
- **Deviations (documented, deliberate):** destest_16 pipes from the case-description Table 6, not the repo CSV (evidence above + DATASET.md); Schutterwald loads use the 50 m service-area cutoff instead of naive all-houses aggregation; Verbier boundary temperature fixed at 5 °C (not in the dataset; ±5 K ⇒ ∓7 % losses) and its 55 aerial pipes share the buried pipes' `text_k` (single-text_k platform convention); DESTEST/Verbier geodata are synthetic anchors over open water (Bodensee/Lac Léman) — local-metre sources carry no CRS.

### 2026-07-17 — Catalog stale-cache fix (branch `fix-catalog-cache`)

- **Bug (confirmed live):** `NetworkCatalog.get_inputs` cached the parsed `NetInputs` per id **forever** — after regenerating `data/networks/verbier/` on disk (geo coordinates changed), `POST /config/apply` on the running backend rebuilt the engine from the STALE cache and `/network` served the old coordinates; only a process restart re-read the files. The same cache feeds `GET /networks/{id}` previews and `POST /loadgen/assign`, so NetzStudio previews were stale too.
- **Fix:** `get_inputs` now validates its cache against an on-disk fingerprint (`_disk_state`: mtime_ns + size of the five contract files, captured *before* the load — a mid-load rewrite re-reads conservatively on the next access) and drops the entry on any difference; `apply_network` additionally passes the new `refresh=True` (unconditional re-read — apply is rare and `engine.reconfigure` rebuilds the whole Simulator, a JSON re-parse is negligible). `user_networks` lazy-rescan semantics untouched; `rescan_user` drops the fingerprint alongside the cache for vanished entries. Repeated previews of an unchanged network still hit the cache (identity-pinned by test).
- **Safety check (agent sweep):** no consumer mutates a loaded `NetInputs` — loadgen `apply_policy` builds new models via `model_dump`/`model_validate`, runtime CRUD writes to `sim.profiles`/net tables only — so cache sharing needs no deep copies.
- **Tests:** `tests/test_catalog_cache.py` — unit (unchanged → same cached object; geo shift and demand change in different contract files → re-read; `refresh=True` forces) + API (apply → shift geo on disk → apply again → `/network` carries the new coordinate; verified failing on the unfixed code with exactly the live symptom, `48.0 == 48.5`). Full suite 197 backend green (195 kept + 2).

### 2026-07-28 — Gamebridge: simgames co-simulation contract v1 (branch `gamebridge`)

**Built** (spec: `simgames/docs/contract/v1.md` — authoritative for the `/gb/*` surface; extends the stepping-only slice): full contract v1 in `api/gamebridge.py`.

- `POST /gb/net/reset` — topology document → `NetInputs` built **directly from the pydantic models** of `doc.native` (the five-file bundle shape, verbatim; documented choice: models + `cross_validate` ARE the five-file contract, no temp-dir round-trip). Everything validated **before** the swap (a 400 leaves the running network untouched). `engine.reconfigure` at the document's `steps_per_day` raster (engine settings copied with the new steps_per_day — contract ticks are engine ticks 1:1), recorder auto-stopped (one recording per configuration), estimation observer **disabled** (the game consumes the contract layer; `POST /estimation/config` re-enables), JIT warmup solve without advancing/publishing (`warmup_solve_ms`; a warmup that ends non-`ok` on all-zero placeholder demand is a warning, not an error). Gb session state (`GbState`) on the runtime `App`: zone→consumer element map, sample-and-hold demand dict, `last_t`/`last_result` cache.
- **Device mapping** (documented in the module): the FIRST device of kind `slack`/`chp`/`heat_pump`/`boiler` binds to the bundle's one slack producer — plant kinds configure the M4 dispatch model (`sim.plant`: pq_ratio/eta/eta_g/t_cold_source); every further plant-kind device becomes an M2/M4 `heat_exchanger` feed-in at its node (dispatched per step via `q_kw` setpoints, starts 0); `storage_heat` becomes an M4 buffer storage (params `e_kwh`/`p_max_kw`). `POST /gb/net/patch` maps `add_device`/`remove_device`/`set_device` onto that CRUD, tolerant per entry (applied ops stay applied).
- **Stepping** (`POST /gb/step` = debug fallback, `WS /gb/ws` = step channel; identical semantics, shared `_gb_step`): idempotent re-send of `last_t` returns the cached result; any other `t ≠ last_t+1` → HTTP 409 / WS `{"status":"error","error":"out_of_order","expected":[...]}` — the one 4xx in the step path. Per step, BEFORE the solve: `weather.temp_c` → the §4.5 override path (heating curve + loss boundary react); `zone_demand` kW→W into the consumer's **DHW profile slot** at the current tick with `q_sh` zeroed (deliberate: the override must never rescale game-owned demand — that's the bypass convention), sample-and-hold with never-seen zones at 0 (the §3.2 floor still applies); `device_setpoints.q_kw` dispatches hx devices (clamped ≥ 0 → `clamped` violation) and is advisory on the slack-bound device (the pressure slack balances the network — realized feed is reported). Then `engine.external_step()` → contract result: `status` ok→converged / degraded→degraded / failed→failed; zones `supplied` 1.0 (0.0 on failed) + detail `{t_supply_c, dp_bar}` at the zone's consumer; devices `output_kw` (slack: realized `q_kw` + t_flow/plift/pump_el/plant metrics in detail; storage: −q_kw with `soc` as 0..1 fraction); `coupling_out` **sign convention**: CHP `p_el_kw = −pq_ratio·q` (negative feeds the grid), heat pump positive `q/COP`; violations `dp_low` (worst-point Δp < `RTHEATFLOW_DP_MIN_BAR`; critical below half), `t_supply_low` (zone below its `t_supply_min_c`), `clamped`. Failed frames mirror the platform's reuse-last-converged policy (stale device values, but `status`/`supplied` carry the gameplay signal). `GET /gb/result/latest` (404 pre-first-step) + idempotent re-send = the crash-recovery path.
- `/gb/version` now reports contract **1.0** and reads `external_clock` from the App settings (was: env singleton — test-isolated settings were invisible). A native `/config/apply`/scenario load **ends the gb session** (`apply_network` clears `app.gb` and restores the configured tick raster — the gb maps point at the old net; stepping again requires a fresh `/gb/net/reset`).
- Shared fixture authored in simgames: `tests/contract/fixtures/heat_fixture.json` + deterministic stdlib-only generator `gen_heat_fixture.py` (3-consumer star, zones hz0–hz2, slack-bound CHP pq 0.5, 96×900 s script with 20–80 kW morning-peak demands, −5→8 °C temp_c ramp, golden: all supplied 1.0, t_supply ∈ [55,95] °C, `coupling_out.slack.p_el_kw` ∈ [−120,−20]; patch probe = boiler hx at n1).
- **Tests: 211 backend** (202 pre-existing kept green — the old no-body `test_gb_endpoints` rewritten, the M6 metadata recipe re-pinned to 0.8.0 — + 10 contract-surface tests: handshake, 400-before-reset, bad-document rejection incl. steps mismatch/unknown zone consumer/power-kind device, reset→step→idempotent→409→latest roundtrip with sign assertions, WS channel incl. out-of-order + malformed-frame error frames, zone sample-and-hold via native `/state`, weather→heating-curve same-step reaction, patch roundtrip + per-entry tolerance, reset clears `last_t`, native swap ends the gb session; the 2 engine-equivalence tests unchanged). Surface re-pinned (65 REST routes + 2 WS, **API 0.8.0**), docs/API.md regenerated. Shared simgames contract suite: `pytest -q -k heat` → 1 passed (spawned real backend on port 8021, schema-validated every WS step result, golden ranges green, patch probe green).
- **Deviations (documented, deliberate):** the pre-v1 no-body `POST /gb/step` (returned the native frame) is superseded by the contract §4 body/result — puppet-mode stepping without a topology document is now a 400 (`test_gb_endpoints` rewritten accordingly); `coupling_in` is accepted and ignored (heat has no `coupling_load` kind in v1); a `q_kw` setpoint on the slack-bound device raises no `clamped` violation (the slack's realized feed is physics, not a bound clamp).

### 2026-07-17 — Backend process-leak investigation + ops hygiene (branch `fix-test-process-leak`)

**Trigger:** after a live session, three orphaned `python -m rtheatflow.main` processes were found alive (spawned within the same second, survived their parent). They did **not** hold port 8001, yet a subsequently started backend seemed to hang *before* binding its port until they were killed, after which a fresh start came up within seconds.

**Finding 1 — no test-fixture leak exists.** The committed suite spawns **no** server subprocess: exhaustive grep over `tests/` and `scripts/` for `subprocess`/`Popen`/`uvicorn`/`multiprocessing`/`os.system`/`Start-Process` is empty; every API test drives the app through the in-process Starlette `TestClient` (`conftest.make_api_client`), and `RealtimeEngine` runs the solve on `asyncio.to_thread`, never a process. The orphans are **ad-hoc dev/verification backends** started detached (`... &` / `start`) whose parent shell exited — a hygiene problem, not a suite bug. (One such live pair was caught mid-investigation, spawned by an agent's `(python -m rtheatflow.main &)` verification one-liner.)

**Finding 2 — mechanism of the *symptom*.** uvicorn runs the app **lifespan startup before binding the socket** (`uvicorn/server.py`: `startup()` calls `await self.lifespan.startup()` at L104, then `loop.create_server(...)` at L142–184, `self.started=True` at L193). So "hangs before binding" = stuck **inside** lifespan startup (`load_network` → `Simulator(...)` net build → catalog/library/`Recorder(...)`), never a socket fight. `Simulator.__init__` does **not** solve — the ~7 s numba JIT is the first `run_step`, which under autostart runs *after* startup in a background thread, so it is not on the bind path. Confirmed separately that a genuine port collision is a **fast** `WinError 10048` + immediate process exit, **not** a hang.

**Finding 3 — the blocking claim did NOT reproduce; root cause is not a shared-resource lock.** Empirically probed on this machine (16 logical CPUs, 31.7 GB RAM, ~11.7 GB free) with instrumented probes (`faulthandler.dump_traceback_later` to catch any real hang; ephemeral ports; a faithful *port-free* predecessor that acquires all lifespan resources then idles/spins/JITs but never binds):
  - **baseline** cold start binds in **4.9–12.1 s** — dominated by the `pandapipes`/`numba`/`llvmlite` import, with large run-to-run variance (matches the M1 benchmark note);
  - **idle** port-free predecessor holding all lifespan-acquired resources → fresh bind 10–13 s, **inside baseline noise** ⇒ no cross-process lock/handle held during lifespan;
  - **3 CPU-spinning** predecessors → fresh bind 7.4 s; **3 real autostart backends** running warm (engine ticking) → fresh bind **5.5 s** ⇒ no CPU/scheduler starvation on 16 cores;
  - **3 concurrent cold starts** → all bind within ~5.5 s wall-clock ⇒ no import/JIT cross-process serialization.
  - **Ruled out** as the blocker: (a) a numba **cache-file lock** — pandapipes uses bare `@jit` **without** `cache=True` (verified in `pandapipes/pf/*`), so there is no on-disk numba cache and no lock; each process JIT-compiles fresh, process-local; (b) an OS **lock/handle** grabbed in lifespan (idle predecessor test); (c) **CPU/memory** starvation (16 cores, 11.7 GB free; spin/real-backend tests); (d) **port collision** as a hang (fast WinError 10048 + exit); (e) the **recordings/data dir** (default `record=false`; `Recorder.__init__` opens no exclusive handle). **Honest conclusion:** no deterministic shared resource makes a *port-free* stray block a fresh backend's pre-bind startup here. The live symptom is most consistent with import-cost latency during a transient whole-system load spike (a full `pytest`, two parallel agents, browser tooling and the strays all resident at once); killing the strays freed memory/CPU and the next import finished promptly. Not guessed further — the above is the ruled-out list the reproduction supports.

**What changed (proportionate; honest that the root cause is "documentation + hygiene", no physics/runtime bug to fix):**
- `src/rtheatflow/proc_guard.py` — stdlib, cross-platform (`Get-CimInstance` on Windows, `ps` on POSIX), **best-effort** (`live_backend_pids()` returns `set()` on any enumeration error; Windows query is pinned to `python*` procs so it can't match its own powershell; POSIX requires the exe to be python so shell wrappers mentioning the marker don't match).
- **Session-scoped autouse leak guard** in `tests/conftest.py` (the original task's guard, scoped precisely): snapshots live `rtheatflow.main` PIDs at session start, **fails the run** if any *new* one outlives it — pins the "suite spawns nothing" invariant so a future live-uvicorn helper that forgets `kill`+`wait` is caught instead of leaking. Pre-existing backends (a dev instance, a parallel agent's) are in the baseline and never flagged; no-ops if enumeration is unavailable (Linux CI safe).
- **Startup preflight** in `rtheatflow.main` — logs a one-line, **log-only** notice naming sibling `rtheatflow.main` PIDs and pointing at `stop_rtheatflow.bat`. It never kills (a deliberate second instance is legitimate); worded as conditional advice for the "if this start hangs before binding" case. Covers every start path (incl. the launcher's backend console), so `start_rtheatflow.bat` was left unchanged. `stop_rtheatflow.bat` already reaps strays by command line (`*rtheatflow.main*`) — the remedy pre-existed; this only adds diagnosis.
- `tests/test_proc_guard.py` — spawns a python process whose argv carries the `rtheatflow.main` marker (no import, no port), asserts the helper detects it and **forgets it after the tree is killed** (so the leak guard neither misses a leak nor false-flags a cleaned-up process).
- **Tests: 199 backend** (197 kept green — the M1–M6 suite + the catalog fix's 2 — + 2 proc_guard); `pytest -q` run **twice** back-to-back — 199 passed both times, and the process table showed **only the user's dev pair** after each run (zero strays). Did **not** touch `network_catalog.py` (the merged `fix-catalog-cache` work owns it).
